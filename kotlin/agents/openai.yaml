interface:
  display_name: "Kotlin conventions"
  short_description: "Kotlin engineering conventions. MUST be loaded before creating or editing any .kt or .kts file, Gradle Kotlin DSL, or Kotlin project structure — Android, Compose Multiplatform, KMP and backend alike."

policy:
  allow_implicit_invocation: true
